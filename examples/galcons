# Component/    parameter   constraint	Comment
# operation	(see below)   range

     1/2            x        1 1
     1/2            y        1 1
     1_2            pa       offset
    2              x        -10 10    # Soft constraint: Constrains the
					# sersic index n to within values
				        # from 0.7 to 5.
    2              y        -10 10    # Soft constraint: Constrains the
					# sersic index n to within values
				        # from 0.7 to 5.
    1              x        -10 10    # Soft constraint: Constrains the
					# sersic index n to within values
				        # from 0.7 to 5.
    1              y        -10 10    # Soft constraint: Constrains the
					# sersic index n to within values
				        # from 0.7 to 5.
#    1              mag        -5 5    # Soft constraint: Constrains the
					# sersic index n to within values
				        # from 0.7 to 5.
#    2              mag        -5 5    # Soft constraint: Constrains the
					# sersic index n to within values
				        # from 0.7 to 5.
    2              rs        -50 50    # Soft constraint: Constrains the
					# sersic index n to within values
				        # from 0.7 to 5.
    1              re        -50 50    # Soft constraint: Constrains the
					# sersic index n to within values
				        # from 0.7 to 5.



# Note on parameter column:
#   The parameter name options are x, y, mag, re (or rs -- it doesn't matter),
#   n, alpha, beta, gamma, pa, q, c, f1a (Fourier amplitude), f1p (Fourier
#   phase angle), f2a, f2p, r5 (coordinate rotation), etc., .  Or
#   alternatively, one can specify the parameter number instead (for the
#   classical parameters only) corresponding to the same numbers in the
#   galfit input file.
